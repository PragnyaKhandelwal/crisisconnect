services:
  - type: web
    name: crisisconnect
    runtime: docker
    plan: free
    healthCheckPath: /api/stats
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
